# ~*~ auto-generated by haskell-ci with config : 07e4d73faf9b86186195cf5644be541b2d5a9bf8f8d82410259bcb1e27af2cf6 ~*~

version: "{build}"
clone_folder: C:\project
build: off
cache:
  - "C:\\SR -> .appveyor.yml"

environment:
  global:
    STACK_ROOT: "C:\\SR"
  matrix:
    - { BUILD: "ghc-8.4", STACKCMD: "stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps", STACKCFG: "{ resolver: lts-12.26, packages: [ '.' ], extra-deps: [], flags: {} }", STACKURL: "https://www.stackage.org/stack/windows-x86_64" }

matrix:
  fast_finish: true

install:
  - set PATH=C:\Program Files\Git\mingw64\bin;%PATH%
  - curl -ostack.zip -L %STACKURL%
  - 7z x stack.zip stack.exe
  - refreshenv
test_script:
  - echo %STACKCFG% > stack.yaml
  - stack setup > nul
  - echo "" | %STACKCMD%


